describe("list permission logic", () => {
  it.todo("can't have more than one owner");

  it.todo("can't have zero owners");

  it.todo("editor can not delete list");

  it.todo("editor can add editor permissions");

  it.todo("editor can only delete their permisson");
});
