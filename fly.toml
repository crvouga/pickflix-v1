# AUTO-GENERATED by scripts/generate-fly.ts — do not edit by hand.
# Regenerate: bun run generate-fly

app = "crvouga-pickflix"
primary_region = "iad"

[build]
  image = "ghcr.io/crvouga/chrisvouga-pickflix:latest"

[[vm]]
  memory = "256mb"
  cpu_kind = "shared"
  cpus = 1

[http_service]
  internal_port = 9000
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 0

  [[http_service.checks]]
    grace_period = "60s"
    interval = "15s"
    method = "GET"
    path = "/"
    protocol = "http"
    timeout = "10s"
