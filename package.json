{
  "scripts": {
    "test": "clear && cd backend && npm test",
    "postinstall": "cd backend && npm install",
    "heroku-postbuild": "cd frontend && npm install && npm run build",
    "heroku-write-env": "heroku config -s -a crvouga-pickflix > .env",
    "start": "cd backend && npm run start",
    "heroku-local": "npm run heroku-write-env && npm install && npm run heroku-postbuild && REPOSITORY_IMPLEMENTATION=postgres PORT=9000 heroku local",
    "heroku-local-production": "NODE_ENV=production npm run heroku-local"
  },
  "name": "pickflix",
  "version": "1.0.0",
  "description": "An app that helps you pick flixs 🍿 to watch. The frontend is a React/Redux app and the backend is an Express REST API.",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/crvouga/pickflix.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/crvouga/pickflix/issues"
  },
  "homepage": "https://github.com/crvouga/pickflix#readme",
  "engines": {
    "node": "12.18.2",
    "npm": "6.14.5"
  }
}
