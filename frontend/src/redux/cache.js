// import { queryCache } from "react-query";

// const actions = {
//   query: createAction("[cache] query", (queryKey, queryFn) => ({payload: {queryKey, queryFn}}))
// }

// function* saga() {
//   yield takeEvery(actions.query, function* (action) {
//     const { queryKey, queryFn } = action.payload

//   })
// }
